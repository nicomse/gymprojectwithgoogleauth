@model IEnumerable<GymProjectWithGoogleAuth.Models.Clases.Profesor>

@{
    ViewBag.Title = "Listar Profesores";
}

<h2>@ViewBag.Title</h2>

<div class="form-horizontal">
    <form action="/" method="post">
        <div class="form-group">
            <div class="col-md-10">
                <input class="form-control" type="text" name="email" value="" id="inputBuscar" placeholder="Ingrese E-Mail" />
            </div>
        </div>
        <button type="button" class="btn btn-primary" id="btnBuscar">Buscar Profesores</button>
    </form>
</div>

<div id="resultados">

</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    <script>
        $('#btnBuscar').on('click', function () {
            var email = document.getElementById('inputBuscar').value
            ListarProfesores(email);
        });

        function ListarProfesores(email) {
            $.ajax({
                url: '/Profesor/BuscarProfesoresAjax',
                data: { 'email': email },
                type: 'post',
                success: function (result) {
                    //devuelve un partial view
                    $('#resultados').empty()
                    $('#resultados').append(result)
                },
                fail: function (result) {
                    console.log(result)
                }
            })
        }
    </script>
}